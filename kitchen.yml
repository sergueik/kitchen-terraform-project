---
driver:
  name: terraform
  directory: .

provisioner:
  name: terraform
  variable_files:
    - variables.tfvars  # You can specify a variables file if you want

platforms:
  - name: ubuntu-20.04
    driver:
      box: ubuntu/20.04

suites:
  - name: default
    run_list:
      - recipe[default]
    attributes: {}
